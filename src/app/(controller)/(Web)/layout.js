import React from 'react';

export const metadata = {
    title: 'Dua & Ruqyah | All Dua Collection',
    description: 'Dua & Ruqyah | All Dua Collection',
  }

const Template = ({ children }) => {
    return (
        <div>
            {children}
        </div>
    );
};

export default Template;
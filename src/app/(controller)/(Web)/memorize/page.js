import Memorize from '@/app/(view)/(web)/Memorize/page';
import React from 'react';

const memorize = () => {
     return (
          <div>
               <Memorize></Memorize>
          </div>
     );
};

export default memorize;